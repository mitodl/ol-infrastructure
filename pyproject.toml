[tool.poetry]
name = "ol-configuration-management"
version = "0.1.0"
description = ""
authors = ["MIT Open Learning Engineering <oldevops@mit.edu>"]

[tool.poetry.dependencies]
python = "^3.7"
pyinfra = { git = "https://github.com/Fizzadar/pyinfra" }
pydantic = "^1.7.3"
httpx = "^0.16.1"

[tool.poetry.dev-dependencies]
pytest = "^6"
wemake-python-styleguide = "*"
black = "*"
copier = "^5.1.0"
pre-commit = "^2.11.1"
mypy = "*"
pytest-testinfra = "^6.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.isort]
profile = "black"
