[Unit]
Description=Restart {{ service_name }} when watched files change
After=network.target
StartLimitIntervalSec=10
StartLimitBurst=5

[Service]
Type=oneshot
ExecStart=/usr/bin/systemctl restart {{ service_name }}.service

[Install]
WantedBy=multi-user.target
