---
opensearch:
  resources:
    requests:
      memory: "1G"
    limits:
      memory: "4G"
airflow:
  dags:
    persistence:
      storageClass: "efs-sc"
  logs:
    persistence:
      storageClass: "efs-sc"
