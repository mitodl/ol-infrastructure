# This file is for configuring the Nginx server block for the xpro application.
server {
    listen 8071 default_server;  # DEFAULT_NGINX_PORT
    server_name _;
    large_client_header_buffers 4 32k;

    root /src;

    # Health check endpoint for Kubernetes probes (doesn't hit Django)
    location = /nginx-health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    location = /.well-known/dnt-policy.txt {
        return 204;
    }

    location ~* /static/hash.txt {
        expires -1;
        add_header Cache-Control private;
    }

    location ~* /static/(.*$) {
        expires max;
        add_header Access-Control-Allow-Origin *;
        try_files /staticfiles/$1 =404;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass 127.0.0.1:8073;  # DEFAULT_WSGI_PORT
        uwsgi_pass_request_headers on;
        uwsgi_pass_request_body on;
        uwsgi_ignore_client_abort on;
        client_max_body_size 25M;
    }

}
