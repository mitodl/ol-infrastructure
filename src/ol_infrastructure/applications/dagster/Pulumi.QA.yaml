---
secretsprovider: awskms://alias/PulumiSecrets
encryptedkey: AQICAHionUR8LBW1ALuVC0rCH3AE2oQIfGMCx3XmpDH9HjM2LQEGccCx6w+aU1z9VNPzXzwIAAAAfjB8BgkqhkiG9w0BBwagbzBtAgEAMGgGCSqGSIb3DQEHATAeBglghkgBZQMEAS4wEQQMkTCVUUXv23ZgiFtbAgEQgDusl6M2Rs0caUtV7jakSKAqsXTqm8f7M0UuEu0hjZ5KdbCp8HUDBShST/yKEpt0ExGXlODrlmL3gGN6fg==
config:
  aws:region: us-east-1
  consul:address: https://consul-data-qa.odl.mit.edu
  dagster:db_password:
    secure: v1:XI74KBKUpbjHPTtE:3ZRZk+kvQ9w92TJPRRC4qw6kdK3etaumBDAgnySVagHjzYgRPfr8MhSeag==
  dagster:domain: pipelines-qa.odl.mit.edu
  dagster:db_instance_type: db.m7g.large
  dagster:edx_pipeline_gcs_courses_bucket_name: edxorg-qa-edxapp-courses
  dagster:edx_pipeline_micromasters_bucket_name: ol-data-lake-mit-open-qa
  dagster:edx_pipeline_micromasters_postgres_db_hostname: micromasters-db-read-replica.cbnm7ajau6mi.us-east-1.rds.amazonaws.com
  dagster:edx_pipeline_micromasters_postgres_db_name: micromasters
  dagster:edx_pipeline_mit_open_bucket_name: ol-data-lake-mit-open-qa
  dagster:edx_pipeline_mit_open_postgres_db_hostname: rc-apps-rds-postgresql-opendiscussions.cbnm7ajau6mi.us-east-1.rds.amazonaws.com
  dagster:edx_pipeline_mitx_enrollments_bucket_name: dagster-data-qa
  dagster:edx_pipeline_mitx_online_edx_course_bucket: mitx-qa-edxapp-courses
  dagster:edx_pipeline_mitx_online_purpose: current-residential-live-mitx
  dagster:edx_pipeline_mitxonline_edx_course_bucket_name: mitxonline-qa-edxapp-courses
  dagster:edx_pipeline_mitxonline_purpose: mitxonline
  dagster:edx_pipeline_residential_mysql_db_hostname: edxapp-db-mitx-qa.cbnm7ajau6mi.us-east-1.rds.amazonaws.com
  dagster:edx_pipeline_xpro_edx_bucket_name: dagster-data-qa
  dagster:edx_pipeline_xpro_edx_course_bucket_name: xpro-qa-edxapp-courses
  dagster:edx_pipeline_xpro_edx_xpro_purpose: mitx-etl-xpro-qa
  # QA's aggregate PgBouncer connection cap is 764 (db.m7g.large, 2 replicas)
  # against production's 4248. At the global default of 100 concurrent runs,
  # QA deadlocked its own pool on 2026-08-17 and stayed wedged for days. 20
  # runs at the ~22 connections apiece measured during that incident is ~440,
  # leaving room for the daemon, webserver and ten code location servers.
  # Revisit once the pgbouncer exporter has enough history to show what a run
  # worker really holds outside a deadlock.
  dagster:max_concurrent_runs: 20
  # event_log_storage's QueuePool ceiling. QA was left on the 10+10 fallback
  # when Production was sized on 2026-08-18, on the stated grounds that it had
  # not shown the failure mode. It had, and still does: at 20:38Z the daemon was
  # logging "QueuePool limit of size 10 overflow 10 reached, connection timed
  # out" continuously -- 100-250 lines per 10 minutes, sustained over at least
  # eight hours, with both user-code servers contributing as well. Same failure
  # Production fixed, never fixed here.
  #
  # 45 rather than Production's 150, by the same per-pod arithmetic in
  # dagster_instance.yaml's event_log_storage comment: worst case the daemon and
  # both webserver replicas peg this ceiling on one pgbouncer pod, which is
  # 3 x (45 + run storage 30 + schedule storage 30) = 315 against QA's 382 cap.
  # That is 82%, deliberately under Production's own 630/708 = 89%, because QA
  # carries risks Production does not: it has already deadlocked its entire pool
  # once from run-worker connections alone, and its per-pod distribution is
  # badly skewed (221 vs 4 measured) where Production's is even (1-18), so the
  # worst case here is a real scenario rather than a pessimistic bound. Measured
  # headroom at the time of writing: 183 and 182 connections per pod of 382.
  #
  # This is sized to be safe, not proven sufficient -- Production needed 7.5x
  # (20 -> 150) and this is 2.25x, and QA runs the same code locations, so if
  # automation-tick concurrency tracks asset count rather than run count it may
  # still be short. DagsterDatabaseConnectionFailuresWarning fires on exactly
  # these log lines and was firing on QA when this was written: if it stays
  # firing once this ships, raise both numbers again.
  dagster:event_log_pool_size: 30
  dagster:event_log_max_overflow: 15
  vault:address: https://vault-qa.odl.mit.edu
  vault_server:env_namespace: operations.qa
