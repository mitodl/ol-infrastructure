# Alerting control-plane drift report

Nightly assertion over the Sentry alert rules, Sentry client keys, Rootly route rules, and Grafana contact points that the Pulumi providers cannot manage. Generated by the `alerting-drift` Concourse job -- see `src/ol_concourse/pipelines/infrastructure/alerting_drift/pipeline.py`. A DRIFT finding is not auto-repaired: decide whether live or the expectation recorded in `bin/alerting-drift-check` is correct, then change whichever one is wrong.

```

KNOWN (2)
  [sentry-rules] Sentry metric alerting
      no metric detectors are declared and none exist in Sentry; all six were deleted on 2026-08-28 because none could fire, and the replacement set is not built yet -- tk-rebuild-sentry-alerting-from-scratch-on-signals--dfd607
  [sentry-liveness] MITOL Sentry -> Rootly
      no Sentry alert can reach Rootly: every metric detector was deleted on 2026-08-28 and none has been rebuilt -- tk-rebuild-sentry-alerting-from-scratch-on-signals--dfd607

INFO (6)
  [grafana-delivery] ci/Adaptive Traces (webhook)
      no notification attempted since the last config reload, so an empty error field proves nothing about whether it works. A contact-point test does not settle it either: that is a separate code path from the notifier and leaves this field untouched
  [grafana-delivery] ci/grafana-default-email (email)
      no notification attempted since the last config reload, so an empty error field proves nothing about whether it works. A contact-point test does not settle it either: that is a separate code path from the notifier and leaves this field untouched
  [grafana-delivery] ci/rootly (webhook)
      no notification attempted since the last config reload, so an empty error field proves nothing about whether it works. A contact-point test does not settle it either: that is a separate code path from the notifier and leaves this field untouched
  [grafana-delivery] ci/slack-devops-warnings-critical (slack)
      no notification attempted since the last config reload, so an empty error field proves nothing about whether it works. A contact-point test does not settle it either: that is a separate code path from the notifier and leaves this field untouched
  [grafana-delivery] ci/slack-devops-warnings-warning (slack)
      no notification attempted since the last config reload, so an empty error field proves nothing about whether it works. A contact-point test does not settle it either: that is a separate code path from the notifier and leaves this field untouched
  [grafana-delivery] qa/grafana-default-email (email)
      no notification attempted since the last config reload, so an empty error field proves nothing about whether it works. A contact-point test does not settle it either: that is a separate code path from the notifier and leaves this field untouched

0 drift, 2 known-defect, 6 not-assertable
```
