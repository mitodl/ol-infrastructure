# GitHub estate drift report

Nightly diff of the committed fleet data in `saas/github/repositories/data/repos/` against live GitHub. Generated by the `github-estate-drift` Concourse job -- see `src/ol_concourse/pipelines/infrastructure/github_drift/pipeline.py`. A finding here is not auto-applied: encode the intended value as a per-repo deviation (if live is correct) or fix it live with `pulumi up` (if the committed value is correct), same as any other phase-5 remediation.

```

0 difference(s) between the committed fleet and /tmp/github-drift-cache.json
```
