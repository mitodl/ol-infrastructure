---
apiVersion: v1
kind: Secret
metadata:
  name: odlvideo-secrets
  namespace: odl-video-service
type: Opaque
stringData:
  SECRET_KEY: "local-dev-odlvideo-insecure-secret-key"  # pragma: allowlist secret
  DATABASE_URL: "postgres://app:localdev@local-pg-rw.local-infra.svc.cluster.local:5432/odlvideo"  # pragma: allowlist secret
  # AWS credentials (stub — transcoding won't work without real AWS)
  AWS_ACCESS_KEY_ID: "local-dev-stub"
  AWS_SECRET_ACCESS_KEY: "local-dev-stub"  # pragma: allowlist secret
